const F1menuText = ``;
   
   let helpMenuOverlay = document.createElement("div");
   helpMenuOverlay.id = "helpMenuOverlay";
   helpMenuOverlay.style.position = "absolute";
   helpMenuOverlay.style.left = "0px";
   helpMenuOverlay.style.top = "0px";
   helpMenuOverlay.style.width = "100%";
   helpMenuOverlay.style.height = "auto";
   helpMenuOverlay.id = "helpMenuOverlay";
   helpMenuOverlay.innerHTML = F1menuText;
   helpMenuOverlay.style.display = "none";
   document.body.appendChild(helpMenuOverlay);
